name: jpzip
description: Dart SDK for the jpzip postal-code dataset (https://jpzip.nadai.dev).
version: 0.1.0
repository: https://github.com/jpzip/dart

environment:
  sdk: ^3.0.0

dependencies:
  http: ^1.2.0
  meta: ^1.10.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
